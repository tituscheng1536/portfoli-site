PORT=3000

# Bearer token required to read GET /api/leads. Set this to something random.
ADMIN_TOKEN=change-me

# Optional: leave blank to skip email notifications on new leads.
# Sent via Resend's HTTP API (not SMTP) since most free hosts block SMTP ports.
RESEND_API_KEY=
NOTIFY_EMAIL=
